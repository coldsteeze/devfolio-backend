services:
  auth-service-uri: ${AUTH-SERVICE-URI}
  user-profile-service-uri: ${USER-PROFILE-SERVICE-URI}

cors:
    frontend-url: ${FRONTEND-URL}

jwt:
  secret: ${JWT_SECRET}
  issuer: ${JWT_ISSUER}
  public-endpoints: ${JWT_PUBLIC_ENDPOINTS}