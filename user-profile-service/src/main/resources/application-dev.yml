spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/user_profile_service
    username: postgres
    password: postgres

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8081
